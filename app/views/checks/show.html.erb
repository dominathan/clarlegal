<% provide(:title, "Check Dates") %>

<%= render 'shared/subheader' %>

<h1>Case Checks</h1>
<table class='table table-hover'>
  <thead>
    <tr>
      <th>Conflict Check Date</th>
      <th>Retention Letter Date</th>
    </tr>
  </thead>
  <tbody>
    <td><%= @check.conflict_check %></td>
    <td><%= @check.retention_letter %></td>
  </tbody>
</table>
<br>
<br>

<h2> <%= link_to "Edit Check Dates", edit_client_case_check_path(@client, @case, @check) %></h2>
