<% provide(:title, "Case Dates") %>

<%= render 'shared/subheader' %>

<h1>Case Timing</h1>

<table class='table table-hover'>
  <thead>
    <tr>
      <th>Date Opened</th>
      <th>Fastest Estimated Conclusion</th>
      <th>Expected Estimated Conclusion</th>
      <th>Slowest Estimated Conclusion</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @timing.date_opened %></td>
      <td><%= @timing.estimated_conclusion_high%></td>
      <td><%= @timing.estimated_conclusion_med%></td>
      <td><%= @timing.estimated_conclusion_low%></td>
    </tr>
  </tbody>
</table>

<h2> <%= link_to "Edit Dates and Timing", edit_client_case_timing_path(@client, @case, @timing) %></h2>
