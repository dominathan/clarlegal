<% provide(:title, "Staff Information") %>

<%= render 'shared/subheader' %>

<h1>Case Staff</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Position</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= Staffing.find_by(id: @staff.staffing_id).full_name %></td>
      <td><%= @staff.position %></td>
    </tr>
  </tbody>
</table>

<br>
<br>

<h2> <%= link_to "Edit Staff", edit_client_case_staff_path(@client, @case, @staff) %></h2>
